# ===== Dify stack (.env example) =====

# Postgres
POSTGRES_DB=dify
POSTGRES_USER=dify
POSTGRES_PASSWORD=P@ssw0rd

# (任意) 上の POSTGRES_* から自動生成する代わりに、
# ここで DATABASE_URL を直指定できます（優先されます）。
# パスワードに @ : / ? # & = などを含む場合は必ずURLエンコードしてください。
# 例: postgresql+psycopg2://dify:pa%40ss%3Aword@postgres:5432/dify
# DATABASE_URL=postgresql+psycopg2://dify:encoded-pass@postgres:5432/dify

# MinIO (S3 compatible)
MINIO_ACCESS_KEY=difyminio
MINIO_SECRET_KEY=P@ssw0rd

# Dify secret key
SECRET_KEY=BOiHDOHFNDJFBOiAASJFNSDNVHPDaJDNVPaIHDVPaDJNEDLNFLABDHPOHADPJADiKVNLDVNLAHDP

# Timezone
TZ=Asia/Tokyo

# Web→API の外向けURL（Cloudflareで dify-api の公開ホスト名を作成した場合に設定）
# 未設定なら内部URL http://dify-api:5001 を使用します。
# 例: https://dify-api.isinxnisi.com
API_URL=
CONSOLE_API_URL=
CONSOLE_WEB_URL=

# Vector store (既定は Qdrant)
VECTOR_STORE=qdrant
# Qdrant
QDRANT_URL=http://qdrant:6333
QDRANT_API_KEY=
# Weaviate（使う場合は VECTOR_STORE=weaviate に変える）
WEAVIATE_ENDPOINT=http://weaviate:8080
WEAVIATE_API_KEY=

# Cloudflare Tunnel（Dify公開用、任意）
CF_TUNNEL_TOKEN_DIFY=
